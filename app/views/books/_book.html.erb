<li>
  <%= link_to image_tag(book.mediumImageUrl, alt: book.title + "画像", title: book.salesDate.to_s + "\n" + book.title + "\n" + number_to_currency(book.itemPrice, precision: 0, delimiter: ",") + "\n" + "レビュー数:" + book.reviewCount.to_s), book.itemUrl, target: "_blank"  %>
</li>
